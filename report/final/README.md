# 最终报告目录

唯一正式报告为 `实验报告.html`，由 12 个章节源和 5 个附录源自动组装。它必须能在断网、移动目录后独立打开，全部图片均已嵌入，并在打印时按章节分页。

```bash
make report-check  # 允许草稿占位符，只检查分章结构和资源
make report        # 拒绝占位符，生成正式单文件
```

不要直接编辑生成的 `实验报告.html`。正文修改必须回到 `report/chapters/` 或 `report/appendices/`，否则下次构建会覆盖改动。
