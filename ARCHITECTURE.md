# 双栅 IGZO 课程项目总体架构

> 当前阶段：T02/T03 数值门已关闭，历史失败保留。M00 R02 只在冻结 IGZO 教学数值域内关闭，R11 36/32/25 只关闭工具/parser 预检。正式 R01 固定为 39/40 E0/FAIL；R02 新命名空间的静态/runner/独立三门为 40/30/24 PASS，E3/E2/E3，路线数值不一致。路线分歧根因 R01 静态合同固定为 38/40 E0/FAIL；R02 静态/探针/独立为 40/30/22 E3/E2/E3，仅在预注册点支持三参数 `limit` 语义诊断。当前 R03 42/30/24 已以 E3/E2/E3 唯一完成，独立复核在冻结 247 行教学域内复现机器精度级路线一致。先提交该 E3，再单独判断 M01 教学边界收口和 C00 开放；正式敏感性、M01、C00、下游和 G0 实验校准边界此刻不变。
> 正式题目：**基于双栅 IGZO TFT 的二维器件模型、紧凑模型与可编程单极性逻辑教学 PDK**。
> 实施原则：普通笔记本可运行、二维优先、证据可追溯、基础闭环先于 HZO 扩展。

> R01 Xyce build/tool 执行已唯一完成但在 SuiteSparse CMake 的 BLAS 发现门以 14/29、独立 9/20 失败；报告、日志、manifest 和部分 cache 保留，R01 不重跑。
> R02/R03/R04 合同唯一检查分别为 22/25、21/25、25/26 E0/FAIL；失败来自候选 token、wrapper 文件名/标记、边界或机器状态字面量断言，均未执行对应构建或模拟器。R05 只修正 R04 残留的 `contract_planned` 状态断言；R06 只冻结 R05 暴露的 M4/Bison/Flex 恢复链。R07 只修正 R06 的 runner 路径字面量断言并换用新根，静态合同 39/39 E3，不改变器件输入或门槛。

## 1. 项目最终要回答什么

1. 双栅偏压怎样改变 IGZO 沟道电势、阈值、跨导和亚阈值特性？
2. 陷阱、接触、几何/介质和温度能否与双栅效应分开识别？
3. 怎样把二维器件结果压缩为 AIM-Spice Level 15 和 ngspice 可用的端口模型？
4. 双栅阈值调控能否构成 IGZO 有源负载单极性逻辑，并量化摆幅、静态功耗、延迟和噪声裕量？
5. 从模型、网表、标准单元、GDS、DRC 到几何 LVS，能否形成可重复的教学 PDK 闭环？
6. 可选 HZO 顶栅能否在不夸大证据的前提下提供可编程阈值或频率状态？

## 2. 固定范围

### 2.1 必做主线

- 单材料 IGZO n 型 TFT。
- 二维单栅/双栅器件仿真。
- IGZO 紧凑模型双轨：ngspice 行为等效与纯源码 Xyce 行为路线；教师指定 AIM-Spice Level 15 路线因未授权来源仅保留历史合同/失败边界，不进入正式数值证据。
- 双栅 IGZO 有源负载 INV、NAND2、NOR2、XOR2。
- 5 级环形振荡器和 1 位全加器。
- IGZO 教学 PDK、PCell、GDS、DRC 和从 GDS 几何提取的 LVS。
- 12 章独立写作源、自动组装的单文件 HTML 报告、原始数据、脚本、运行命令和证据矩阵。

### 2.2 可选扩展

- HZO 准静态回滞和极化引起的阈值偏移。
- 有时域数据后再做 NLS 动力学。
- 简化寄生提取与前后仿真对比。
- 工艺/模型参数 Monte Carlo。

### 2.3 明确不做

- 三维全器件或全芯片 TCAD。
- 没有数据支撑的“精确双栅实验拟合”。
- 未定义工艺层的复杂多层金属布线。
- 把行为模型称为原生 Level 61。
- 把教学 DRC/LVS 称为代工厂签核。

## 3. 总体依赖图

```text
S00 数据与来源冻结
  |
  +--> T00 二维静电基准（已有 E2，可复用）
  |      |
  |      +--> T01 单栅漂移扩散
  |              |
  |              +--> T02 双栅电流与阈值耦合
  |                      |
  |                      +--> T03 五组器件参数分析
  |
  +--> M00 IGZO 紧凑模型标定 <--------- T01/T02/T03
          |
          +--> M01 ngspice/Xyce 交叉对照（AIM-Spice 失败路线仅作历史归档）
                  |
                  +--> C00 有源负载反相器
                          |
                          +--> C01 NAND2/NOR2/XOR2
                                  |
                                  +--> C02 五级环振
                                  +--> C03 一位全加器
                                          |
                                          +--> L00 PCell/GDS
                                                  |
                                                  +--> V00 DRC
                                                  +--> V01 几何 LVS
                                                          |
                                                          +--> PEX0 可选后仿真

T02 + 文献参数 --> FE0 可选 HZO 扩展

所有阶段 --> R00 分章写作、单文件 HTML 报告与工程交付
```

不能跨过依赖门。例如没有通过 C00，就不能把环振不振荡归因于寄生；没有 T02 的阈值耦合，就不能声称有源负载的第二栅已物理标定。

## 4. 阶段合同

| ID | 阶段 | 输入 | 必须输出 | 进入下一阶段的门槛 |
|---|---|---|---|---|
| S00 | 数据审计 | 老师数据、论文、工艺、学长参考 | manifest、单位表、冲突表、数据等级 | 每个数有来源、单位和条件 |
| T00 | 双栅静电 | 几何、介电常数、边界和两档网格 | 电势图、耦合曲线、网格比较、JSON | 网格差不超过 5 mV，方向正确 |
| T01 | 单栅漂移扩散 | IGZO 材料、载流子、接触和偏压 | Id-Vg/Id-Vd、二维状态、守恒、失败点 | 至少一个条件收敛，端口电流守恒 |
| T02 | 双栅电流 | T01 模型、顶/底栅扫描 | 曲线族、Delta VTH、gm、内部状态 | T02-A/B/C 均已 PASS；完整数值门只对冻结教学模型关闭并允许进入 T03 |
| T03 | 五组参数 | T02 基线 | 统一敏感性表、机制解释 | P1/P2/P3/P4/P5 均有正式运行与独立落盘检查，历史失败保留；只关闭冻结教学模型内的数值五组并允许建立 M00 合同 |
| M00 | 紧凑模型 | 条件完整 I-V 或合格 TCAD 曲线 | 参数、训练/验证划分、残差和适用域 | 线性/对数、多 VDS 误差都报告 |
| M01 | 双轨对照 | 同一参数目标和偏压 | 两套模型叠图、指标差异、限制 | 不混淆模型方程与证据等级 |
| C00 | 反相器 | 双栅模型、VDD、负载和尺寸比 | VTC、VOH/VOL、VM、增益、NM、延迟、功耗 | 电平可区分，静态/瞬态条件完整 |
| C01 | 基础门 | 已通过 C00 的负载/驱动规则 | 三个门的真值表和路径延迟 | 所有组合通过，内部节点不浮空 |
| C02 | 环振 | 5 个同版 INV、负载和启动条件 | 波形、频率、每级延迟、功耗 | 稳态至少 10 周期，周期波动有界 |
| C03 | 全加器 | 2 XOR2 + 3 NAND2 | 8 行真值表、最坏延迟和功耗 | 8/8 通过，采样避开边沿 |
| L00 | 版图 | 冻结网表、层表、端口和设计规则 | 层次 GDS、面积、端口和器件清单 | 真实器件与连线，不允许占位矩形 |
| V00 | DRC | GDS 和同一 DRC deck | 正常 PASS、故障 FAIL 和数据库 | 正常 0 违例，窄线故障可检出 |
| V01 | LVS | GDS、提取规则和参考 SPICE | 提取网表、比较报告和故障注入 | 正常 PASS，断路/短路/错标均 FAIL |
| PEX0 | 寄生 | 验证后 GDS 和近似参数 | RC/C 网表、前后仿真差异 | 参数来源和近似边界明确 |
| FE0 | HZO 扩展 | T02 模型和文献回滞参数 | 回滞、Delta VTH、可编程状态 | 关闭铁电后严格回到双栅基线 |
| R00 | 报告 | 全部证据包 | 12 章源、章节清单、单 HTML、工程包、复现说明 | 分章顺序正确、打印分页、图片内嵌、无占位符 |

## 5. 五组器件参数

老师要求的高难度五组固定为：

1. `P1 双栅`：顶/底栅偏压及上下栅电容比。
2. `P2 陷阱`：带尾态、深态和界面陷阱。
3. `P3 接触`：源漏接触电阻或势垒。
4. `P4 几何/介质`：L、沟道厚度、物理介质厚度和介电常数。
5. `P5 温度`：至少 250 K、300 K、350 K 或有数据支持的三个温度。

电路层的负载栅偏压、`Wload/Wdriver`、VDD 和 Cload 是额外第六组，不替代器件五组。

## 6. 单极性逻辑架构

### 6.1 主拓扑

使用双栅 IGZO 有源负载：

- 驱动管由输入控制主栅，负责下拉。
- 负载管通过第二栅调节等效阈值，负责上拉。
- 这是有比例逻辑，低输出时可能存在直流通路。
- 必须扫描负载/驱动强度比，不能沿用互补逻辑的静态功耗结论。

### 6.2 固定层次和器件数

| 单元 | 结构 | IGZO TFT 数 |
|---|---|---:|
| INV | 1 个有源负载 + 1 个驱动 | 2 |
| NAND2 | 1 个有源负载 + 2 个串联驱动 | 3 |
| NOR2 | 1 个有源负载 + 2 个并联驱动 | 3 |
| XOR2 | 4 个 NAND2 层次实例 | 12 |
| RING5 | 5 个 INV | 10 |
| FULL_ADDER | 2 XOR2 + 3 NAND2 | 33 |

### 6.3 降级路径

若双栅有源负载模型尚未收敛，可用理想电阻负载检查 Boolean 拓扑和仿真脚本，但必须标成 `solver_smoke`，不能用于 PDK、功耗、面积或器件性能结论。

## 7. 数据接口

### 7.1 原始器件表最低字段

```text
dataset_id, device_id, sweep_id, VGS_V, VDS_V, ID_A,
W_um, L_um, temperature_K, sweep_direction, source, source_status
```

双栅数据额外包含：

```text
VBG_V, VTG_V, bottom_dielectric_nm, top_dielectric_nm,
bottom_dielectric_k, top_dielectric_k
```

### 7.2 参数主表最低字段

```text
parameter, symbol, value, unit, layer_or_model,
source_type, source_reference, fitted_or_fixed, valid_range, uncertainty
```

### 7.3 每个实验的证据包

```text
input/        原始输入或输入哈希
config/       本次实际参数快照
raw/          求解器原始输出
processed/    规范 CSV/JSON
figures/      从 processed 重画的图
report.json   工具版本、命令、耗时、PASS/FAIL 和限制
```

## 8. 目录职责

```text
config/                 全局范围、实验矩阵、TCAD 参数
data/raw/               只读来源与哈希
data/processed/         清洗、统一单位和可拟合表
tcad/                   二维结构、方程、案例和测试
models/level61/         AIM-Spice Level 15 参数与映射
models/dual_gate/       双栅耦合和阈值调节模型
models/ferroelectric/   可选 HZO 回滞/NLS
spice/models/           ngspice 行为模型
spice/netlists/devices/ 单管测试
spice/netlists/cells/   INV/NAND2/NOR2/XOR2
spice/netlists/blocks/  RING5/FULL_ADDER
pdk/tech/               层表、工艺与端口约定
pdk/pcells/             IGZO TFT 和标准单元生成器
pdk/drc/                教学 DRC
pdk/lvs/                器件识别、连通和比较
layout/cells/           单元中间版图
layout/blocks/          层次顶层版图
layout/gds/             验证后 GDS/OASIS
verification/           DRC/LVS/仿真及故障注入
results/                统一图、表和机器报告
report/                 章节/附录源、组装清单、证据矩阵和最终单 HTML
```

源码与生成物不能混放。`data/raw/` 不手改，`results/` 不作为模型输入的唯一来源。

## 9. 命名和版本规则

- 实验：`T01`, `M00`, `C00` 等固定 ID。
- 数据：`<experiment>_<device>_<sweep>_<revision>.csv`。
- 参数：`<model>_<dataset>_<revision>.json`。
- 单元：`INV_DG`, `NAND2_DG`, `NOR2_DG`, `XOR2_DG`。
- 顶层：`RING5_DG`, `FULL_ADDER_1BIT_DG`。
- 结果图必须含实验 ID，不能只写 `result_final.png`。
- 参数或模型改变即升 revision，旧结果不得静默覆盖。

## 10. 证据状态

| 内容 | 当前等级 | 当前状态 |
|---|---|---|
| 来源/课程要求/学长资料索引 | E3 | 已有哈希和自动检查 |
| 二维双栅静电基准 | E2 | 已运行，可复用但不能预测电流 |
| 条件不完整的学长 IGZO 曲线 | E1 | 仅参考，不用于精确拟合 |
| T01-A 输入合同 | E3 | 单栅电子输运输入已冻结；合同本身不运行仿真 |
| T01-B 低偏压漂移扩散烟雾 | E2 | 两档网格下零偏压与 VGS=0 V、VDS<=0.01 V 均收敛，端口电流守恒通过；不等同完整 I-V |
| T01-C 低 VDS 栅压续算 | E2 | VDS=0.01 V、VGS=-1.0 至 1.0 V 的两档网格续算、守恒、单调性和状态保存通过；高正栅压绝对电流网格差最高 27.5%，不得用于定量拟合或 Ion/Ioff |
| T01-D-A 界面法向网格收敛 | E2 | 固定物理输入和横向/体区网格，界面窗口 1x/2x/4x/8x 加密；4x/8x 在 VDS=0.01 V、VGS=0.5/1.0 V 的最大电流差 0.01639%，只打开 Id-Vd 子阶段 |
| T01-D-B 离散 Id-Vd 曲线族 | E2 | interface_4x 的 4 条正式曲线与 interface_8x 的 2 条复核曲线共 30 点全部收敛、守恒且单调；只打开状态/提取子阶段 |
| T01-D-C 状态与受限提取 | E2 | 4x/8x 共 120 次 DC、102 点收敛；三状态含电势、电子浓度和局部电流密度，数值代理网格比较通过 |
| 完整 T01 漂移扩散数值门 | E2 | 冻结教学模型阶段门已关闭并打开 T02；不等同实验标定、物理 Ion/Ioff 或参数不确定度验证 |
| T02-A 顶栅合同与极限回归 | E2（合同 E3） | 14 次 DC 全部收敛；移除顶栈的 7 点对 T01 最大电流相对差 7.13e-15，启用顶栈全零偏压平衡态通过，独立 14 项检查 PASS；非零双栅耦合仍未验证 |
| T02-B 最小正向顶栅族 | E2（合同 E3） | VTG=0/0.1/0.2/0.3 V 的 9 次 DC 全部收敛，电流/中心势/密度严格增加，端点电流比 9.6802，独立 14 项检查 PASS；只打开 T02-C |
| T02-C 双向曲线与受限提取 | E2（合同 E3） | 318 次 DC、248 点和 6 状态通过；VTH/Delta VTH/gm/耦合、回程和上下栅互易由独立 17 项复算确认；只关闭冻结模型 T02 数值门 |
| T03-P4-L 沟道长度敏感性 | E3 | V2 合同 25 项、运行器 16 项、独立 14 项 PASS；L=8/10/12 um 共 123 次 DC/93 点/3 状态。理想 1/L 诊断 FAIL 且不是完成门 |
| T03-P1-BIAS 固定底栅偏压敏感性 | E3 | 合同 22 项、运行器 14 项、独立 14 项 PASS；VBG 五点共 217 次 DC/155 点/5 状态。对称 Ctop/Cbottom=1 只作固定输入代理，未扫描电容比 |
| T03-P1-CAP-RATIO 有效电容分配比敏感性 | E3 | 合同 20 项、运行器 16 项、独立 13 项 PASS；固定总耦合下五个比值共 205 次 DC/155 点/5 状态。成对介电常数只作差分耦合编码，不是材料测量 |
| T03-P2-DIT 界面敏感性子阶段 | E3（运行 E2） | 先完成方程冒烟；正式零控制 + 3 点扫描为 4 器件/164 次 DC/124 点/4 状态，合同 21 项、运行器 14 项、独立 16 项 PASS |
| T03-P2-BULK-TRAPS 输入与方程冒烟 | E3（运行器 E2） | 30/30 静态合同；3 器件/21 次 DC 的 NTA/NGA 方程冒烟和独立 16 项检查 PASS；该行本身只关闭方程冒烟 |
| T03-P2-BULK-TRAPS 正式 V1 失败 | E0/FAIL | 8 器件、328 次收敛 DC、248 点、8 状态、48 VTK 已完成；最高 NTA 未包络冻结 VTH 判据，非零陷阱近零内部电势门不适用；输入、源码与全部结果版本化保留 |
| T03-P2-BULK-TRAPS 正式 V2 合同 | E3（仅静态） | 23/23 PASS；密度点和阈值不变，共同栅压扩展至 1.7 V，近零内部电势只检查零控制；冻结 8 器件/440 DC/360 transfer 点计划，`NOT_RUN_BY_CONTRACT_CHECK` |
| T03-P2-BULK-TRAPS 正式 V2 失败 | E0/FAIL | 8 器件/440 次收敛 DC/360 点/8 状态/48 VTK 已完成；最高 NTA 已包络 VTH，但 1.6666676 V gm 评价点缺少中心差分上邻点；失败包保留，独立检查未运行 |
| T03-P2-BULK-TRAPS 正式 V3 合同 | E3（仅静态） | 24/24 PASS；共同上限统一扩展至 1.8 V，其他输入和阈值不变；冻结 8 器件/456 DC/376 transfer 点计划，`NOT_RUN_BY_CONTRACT_CHECK` |
| T03-P2-BULK-TRAPS 正式 V3 敏感性 | E3（运行器 E2） | 8 器件/456 次收敛 DC/376 点/8 状态/48 VTK；运行器 17/17、独立 16/16 PASS。NTA 与 NGA 的 VTH/gm/SS/电流/状态均为数值代理；NGA SS 轻微递减只作诊断 |
| T03-P2 陷阱组（整体） | E3 | bottom-interface DIT 和 bulk 正式敏感性均有独立落盘证据，V1/V2 失败继续保留；只关闭冻结准静态教学模型内的 P2，不验证测量/拟合 DOS、动态陷阱或实验参数 |
| T03-P3 接触 V1 失败 | E0/FAIL | 12 器件/243 次收敛 DC/156 点/3 状态/18 VTK 已完成；runner 24/25，唯一失败是零漏压相对守恒适用域；V1 独立检查未运行，证据完整保留 |
| T03-P3 接触 V2 恢复合同 | E3（仅静态） | 34/34 PASS；三点、物理/数值模型、偏压、提取、阈值和 12/243/156 预算不变，只分离零/非零漏压门并使用独立路径；未运行 V2 DEVSIM |
| T03-P3 接触 V2 正式敏感性 | E3（运行器 E2） | 12 器件/243 次收敛 DC/156 点/3 状态/18 VTK；运行器 25/25、独立 20/20 PASS。最大输入的高栅电流代理下降 0.161396%；只关闭数值 P3，不是物理接触验证 |
| T03-P5 温度正式合同 | E3（仅静态） | 23/23 PASS；冻结 250/300/350 K、`V_t=k_B*T` 唯一温度项、300 K T02-C 回归和 3 器件/123 DC/93 点/3 状态/18 VTK 计划；未运行 DEVSIM |
| T03-P5 温度正式敏感性 | E3（运行器 E2） | 3 器件/123 次收敛 DC/93 点/3 状态/18 VTK；运行器 14/14、独立 15/15 PASS，300 K 对 T02-C 完全复现。只改变 `V_t`，不证明物理温度模型 |
| T03 五组参数（整体） | E3 | 数值 P1/P2/P3/P4/P5 全部完成，历史失败保持可审计；只关闭冻结未校准教学模型内的五组数值敏感性 |
| IGZO 紧凑模型多曲线标定 | R02 runner E2；独立检查 E3 | R01 唯一运行 21/24、E0/FAIL并完整保留；R02 27/27 静态 PASS、runner 24/24、独立检查 20/20，固定一阶长度因子、10 个系数且原 split/门槛不变。仅关闭冻结教学数值域；候选已生成但 SPICE 未运行 |
| M01 双仿真器对照合同 | E3 静态 | revision-3 32/32 PASS；247 选定行（233 scored + 14 审计）、13 曲线、同几何/偏压/温度/端口映射、两条候选路线、工具指纹、语法/差异/失败保留和 no-circuit 边界冻结；模拟器未运行，v1/v2 失败报告保留 |
| M01 R01 工具/来源预检 | E0/FAIL | 唯一运行 11/13；ngspice 三门通过，AIM-Spice 文件指纹通过但未启动，授权来源与 batch/CLI 两门失败。报告/原始日志保留，10 个数值输出全缺失，不是器件结果 |
| M01 开源第二仿真器恢复合同 | E3 静态 | 30/30 PASS；保留 revision-3 合同、R01 工具/来源失败和一次 28/30 干跑失败；冻结 ngspice + 纯源码 GPL-3.0-or-later Xyce 7.10.0、同一 IGZO 行为候选、批处理模板、失败保留和 no-device-execution 边界。源码审阅不是 Xyce 二进制、器件网表或数值结果 |
| M01 Xyce 构建/工具预检合同 | E3 静态 | 25/25 PASS；固定四个官方来源包和实际 SHA-256、用户目录 serial 两任务构建、MPI/Fortran 关闭、SuiteSparse AMD + Trilinos + Xyce 顺序、版本/许可证指纹、标量 B-source 自测、随后 `-syntax` 解析冻结 IGZO 候选、排他输出和独立 20 项复核。旧恢复合同的 hash 转录差异登记但不改写；Xyce 尚未构建/运行 |
| M01 Xyce 构建/工具预检 R01 执行 | E0/FAIL | R01 runner 14/29、独立检查 9/20；SuiteSparse CMake 未找到显式用户目录 BLAS/LAPACK，停止于构建前段。报告、日志、manifest 和部分 cache 保留；无 Xyce 二进制、自测、器件 DC 或正式数值输出 |
| M01 Xyce 构建/工具预检 R02 合同 | E0/FAIL | 22/25 静态检查；显式 BLAS/LAPACK 和 R01 失败绑定通过，候选 `translation` 被 `tran` 子串规则误判、wrapper 静态标记缺失。无 R02 构建、Xyce、自测、器件 DC 或正式数值输出 |
| M01 Xyce 构建/工具预检 R03 合同 | E0/FAIL | 合同已建立并唯一检查 21/25；固定 token-safe 候选范围、完整 no-formal wrapper 标记、显式 BLAS/LAPACK 和独立 `r03` 根；R01/R02/R03 失败不可改写。无 R03 构建、自测、器件 DC 或正式数值输出 |
| M01 Xyce 构建/工具预检 R03 合同失败 | E0/FAIL | 21/25 静态检查；四项失败是 R03 checker 的 planned-state、wrapper 文件名和边界字面量断言。报告不可改写；无构建、Xyce、自测、器件 DC 或正式数值输出 |
| M01 Xyce 构建/工具预检 R04 合同 | E0/FAIL | 唯一静态检查 25/26；只因 checker 仍期待 `preflight_planned` 而实际机器状态为 `contract_planned`。报告不可改写；无 R04 构建、自测、器件 DC 或正式数值输出 |
| M01 Xyce 构建/工具预检 R05 | E0/FAIL | 静态合同 27/27 E3；runner 唯一运行 19/29。SuiteSparse/Trilinos 安装、Xyce 配置通过，Xyce build 因 M4/Bison 数据路径缺失停止；无二进制、自测、候选解析、器件 DC 或数值输出 |
| M01 Xyce 构建/工具预检 R06 合同失败 | E0/FAIL | 唯一静态检查 36/37；checker 错误禁止独立 checker 为哈希绑定所需的 runner 文件名字面量。其余 36 项通过；0 个构建/模拟器进程，无网表或数值输出，报告不可改写 |
| M01 Xyce 构建/工具预检 R07 静态合同 | E3，39/39 PASS | 实施提交 `d421277` 后唯一检查；0 个构建/模拟器进程，无器件网表和数值输出。只打开 R07 runner 门 |
| M01 Xyce 构建/工具预检 R07 runner | E0，42/47 FAIL | M4/Bison/Flex 与 Xyce 7.10.0 构建/安装通过；B-source `.prn` 含 1.25 V，但 runner 预注册 `.csv` 并错误使用 CSV parser，停止 parser-only。完整失败证据保留，独立检查未运行 |
| M01 路线分歧根因 R01 | E0，38/40 STATIC FAIL | 实施提交 `015253f` 后唯一静态检查；因读取 R02 runner 报告不存在的 `route_diagnostics` 主门失败，最终汇总门连带失败。零模拟器/网表/数值输出；R01 冻结，R02 仅修正证据来源，根因探针未运行，M01/C00 关闭 |
| M01 路线分歧根因 R02 实施 | E0，静态未运行 | 新命名空间绑定提交 `203acaa` 与 R01 38/40 失败，静态 checker 改从三份实际哈希绑定 CSV 重算路线诊断并按状态生成下一门；40/30/22、探针、工具、阈值和失败保留不变。`make check` 756/756，报告结构检查 PASS；无静态报告、网表、进程或新增数值输出 |
| M01 路线分歧根因 R02 静态合同 | E3，40/40 PASS | 提交 `91fe397` 后唯一执行；绑定 R01 38/40 失败和三份诊断 CSV，重算观察值并保留 40/30/22、探针、工具、阈值和失败门。报告 SHA-256 `2870566c...21169`，零模拟器/网表/数值输出；只打开提交后的探针门，不是根因、路线一致或物理校准证据 |
| M01 路线分歧根因 R02 最小探针 | E2，30/30 PASS | 静态 PASS 提交 `283cf32` 后唯一执行一个 ngspice 与一个 Xyce 串行进程；26 行表确认 ngspice 三参数 `limit` 与 clamp/Xyce 语义分歧，portable 候选及支路哨兵通过。只支持预注册最小点根因假设 |
| M01 路线分歧根因 R02 独立复核 | E3，22/22 PASS | E2 提交 `406cfce` 后唯一执行，零进程再生两网表、重算 9 哈希与 26 行表并复现分类。只关闭最小根因诊断；完整 247 行 portable 候选尚未建立或执行，M01/C00 继续关闭 |
| M01 正式开源两路线器件 DC R03 静态合同 | E3，42/42 PASS | 提交 `50066b7` 后唯一执行；绑定 R02 器件/根因链、247 行、五处可移植替换、42/30/24 注册、两进程预算、排他输出和失败保留。报告 SHA-256 `a30611ad...c0237a`，零 build/simulator process、零器件网表、零数值输出；只打开提交后的两路线 runner |
| M01 正式开源两路线器件 DC R03 runner | E2，30/30 PASS | 静态 PASS 提交 `e664629` 后唯一执行；恰好一个 ngspice 与一个 GPL Xyce 串行进程返回 0，两个 247 器件 ASCII 网表、247+247 行、30 指标、247 差异、14 哈希和两图落盘。最大绝对/对数差 `4.37069e-19 A/cm`/`2.75335e-14 decade`；只打开提交后的 24 项独立复核，不建立方程身份、物理参数、校准、M01/C00 或电路证据 |
| M01 正式开源两路线器件 DC R03 独立复核 | E3，24/24 PASS | runner 提交 `6f4e89b` 后唯一执行且零进程；独立再生两个 247 器件网表，解析 raw/PRN，精确重算两路 247 行、30 指标、247 差异、14 哈希和两图尺寸。报告 `5419f34b...a937`，复现机器精度级一致；M01/C00 仍待本 E3 提交后的单独收口决定 |
| 有源负载单元与模块 | E0 | 架构已定，待实现 |
| IGZO 器件 GDS 基线 | E2 | 外部基线可复用，需迁入新 PDK |
| 电路级 DRC/LVS | E0 | 待实现 |
| HZO 扩展 | E0/E1 | 只有文献和计划 |

## 11. 架构完成判据

当前“总体架构确定”只有在以下条件同时满足时成立：

- 活动配置只定义 IGZO 器件和单极性逻辑。
- 所有阶段有唯一 ID、依赖、输入、输出和验收门。
- 电路拓扑与器件数固定。
- 目录职责和数据接口固定。
- 旧范围结果不再列为当前电路证据。
- README、计划、状态、报告标题和 AI 上下文一致。
- 结构检查通过，但不以此冒充领域仿真通过。

## 12. 架构确认后的执行顺序

1. S00：向老师确认主 IGZO 数据集与双栅数据边界。
2. T01：只做单栅漂移扩散最小收敛案例。
3. T03：在已通过的 T02 基线上逐组完成双栅、陷阱、接触、几何/介质和温度敏感性，每次只改变一个变量；五组数值门均已关闭，历史失败继续保留。
4. M00/M01：M00 历史失败与 R02 受限关闭均保留。M01 历史证据不可改写；R11 36/32/25 完成且不重跑，正式器件 DC R01 固定为 39/40 E0/FAIL。R02 40/30/24 已完成并独立确认路线分歧；根因 R01 已实现三表达式点、支路哨兵和原始/可移植候选的最小 40/30/22 链。先提交实施态再运行纯静态合同；静态 PASS 状态提交前不运行探针，电路继续关闭。
5. C00：先把一个有源负载反相器做完整。
6. L00/V00/V01：对同一个 INV 完成 GDS/DRC/LVS 最小闭环。
7. 扩展基础门、环振和全加器。
8. 时间允许再做 FE0，不让可选扩展阻塞基础闭环。
