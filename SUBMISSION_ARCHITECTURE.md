# 提交版项目架构

本文件定义本次课程提交的有效范围。它优先于历史规划图中的未来工作包；历史配置、脚本和失败归档继续保留，仅用于复现与审计。

```text
S00 来源与单位审计 (E3)
  -> T01 单栅二维漂移扩散 (E2)
  -> T02 双栅耦合与双向偏压 (E2)
  -> T03 五组教学模型敏感性 (E3)
  -> M00 教学紧凑候选 (E2/E3, limited)
  -> M01 ngspice/GPL-Xyce 对照 (E3, limited)
  -> C00 双栅 IGZO 有源负载 INV (E0/FAIL, G3 closed)
  -> R00 HTML/PDF/PPT 交付
```

## 已保留的结果

- 仅 n 型 IGZO、二维、普通笔记本可运行的教学模型。
- T01--T03 的数值结果、输入快照、状态、图表、独立检查及历史失败。
- M00/M01 的受限教学模型结果与两开源路线证据。
- C00 R04 的完整双路线负结果：4 个串行进程均完成，但冻结锚点最大增益约 0.884、无有效逻辑 crossing，因此不得称为反相器通过。
- 自包含 HTML、PDF、PPTX、证据矩阵和 Git 哈希链。

## 本次明确不纳入

- C01--C03：NAND/NOR/XOR、环振、全加器。
- L00/V00/V01：PCell、GDS、DRC、LVS。
- PEX0：寄生提取与后仿真。
- FE0：HZO/铁电/NLS 扩展。

这些内容没有正式运行结果。它们的目录、历史计划和机器配置仅作为未实施的归档脚手架保留，不是提交证据，也不应在答辩中列为“进行中”或“已完成”。

## 目录口径

| 目录 | 提交作用 |
|---|---|
| `data/`、`config/` | 冻结输入、来源与可审计配置 |
| `tcad/`、`models/`、`spice/models/` | 已完成二维与教学模型实现 |
| `results/` | 正式 PASS/FAIL 报告、表和原始产物 |
| `report/`、`ppt/` | 最终 HTML、PDF、PPTX 与构建脚本 |
| `layout/`、`pdk/`、`verification/`、`spice/netlists/` | 未实施脚手架，不属于本次交付结果 |

## 表述边界

本项目不主张实验校准、物理参数提取、原生 Level 61、可流片 PDK、合格逻辑单元、版图签核或 HZO 结果。C00 的失败是可复现设计筛选结论，不是工具故障。
